name: lab03-dl-2026-01

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip>=24

  - pip:
      - torch>=2.2
      - torchvision>=0.17
      - numpy>=1.26
      - Pillow>=10.0
      - matplotlib>=3.8
      - streamlit>=1.32
      - opencv-python-headless>=4.9
      - scikit-learn>=1.4
      - pytest>=8.0
      - tqdm>=4.66
      - threadpoolctl>=3.1
